# Changesets

This folder contains release notes generated for `@codagent-ai/agent-plugin`.
Run `npm run changeset` to add an entry, then `npm run version` to consume
pending changesets and bump the package version.
